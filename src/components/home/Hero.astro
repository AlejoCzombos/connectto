---
import Section from "../UI/Section.astro";
import SecondaryButton from "../UI/SecondaryButton.astro";
import PrimaryButton from "../UI/PrimaryButton.astro";

import json from "../../data/home.json"
const {subtitle, title} = json.hero
---

<Section class="flex flex-col xl:flex-row gap-4 xl:gap-8">
    <div class="flex flex-col justify-center order-last xl:order-first xl:w-[50%] gap-4">
        <header class="space-y-3 xl:space-y-5 text-white">
            <h2>{title}</h2>
            <p set:html={subtitle}></p>
        </header>
        <footer class="flex flex-wrap gap-3">
            <PrimaryButton href="/planes" text="Empresas"/>
            <PrimaryButton href="/planes" text="Plan Gamer"/>
            <SecondaryButton href="/planes" text="Ver planes"/>
        </footer>
    </div>
    <img class="rounded-3xl w-full max-w-3xl m-auto xl:w-[50%] max-h-96 object-cover object-top" src="/images/imagen-hero.png" alt="Imagen hero">
</Section>