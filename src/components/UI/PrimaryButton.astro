---
const {href, text, class: className} = Astro.props;
---

<a
    class:list={["py-2 px-4 md:px-6 text-center text-white font-semibold bg-secondary-600 rounded-lg hover:scale-110 transition-transform duration-300 ease-in-out", className ]}
    href={href}
>{text}</a>